# Flask Fly.io Deployment

This repository contains a simple Flask app to handle a callback endpoint.

## Setup

1. Clone the repository:
   ```sh
   git clone https://github.com/yourusername/flask-fly-deploy.git
   cd flask-fly-deploy
   
